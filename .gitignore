# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Hackathon ignored files
screenshots/
recordings/
reference/
*.webp
!public/*.webp

# Python / Virtual Environment
venv/
.venv/
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints/
.env
*.env
.pytest_cache/
*.db
install.log
server.log
ml/train.log

# ML: commit ONLY the small trained model (not reproducible without retraining).
# Everything else is deterministic/derived and regenerated at build time by
# `python -m ml.prescore` (which runs generate_synthetic first): raw data CSVs,
# the betti cache, and the prescored scored.csv/graph.pt.
ml/data/
*.csv
*.pkl
*.joblib
ml/artifacts/graph.pt
!ml/artifacts/metrics.json
!ml/artifacts/model.pt
!ml/artifacts/scaler.npz
!ml/artifacts/shap_background.npy

.vercel
npm-install.log
*.stackdump
sp.json*
